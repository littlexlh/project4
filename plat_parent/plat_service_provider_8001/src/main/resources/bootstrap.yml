#配置本地的配置中心地址和github相关信息
spring:
  cloud:
    config:
      uri: http://127.0.0.1:1299 #配置服务器
      label: master #分支
      name: application-plat-service #github上面名称
      profile: test #环境:在达成jar包运行的时候,动态传入参数