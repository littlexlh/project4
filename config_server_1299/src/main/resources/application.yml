server:
  port: 1299
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    prefer-ip-address: true
spring:
  application:
    name: project4-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/littlexlh/project4_config.git  #配置文件仓库地址
          username: littlexlh
          password: cl507143878